# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง
PORT=8091

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=timecarehub
DB_PASS=ใส่รหัสผ่านตรงนี้
DB_NAME=timecarehub

# ความลับสำหรับเซ็น JWT — สุ่มมาเอง เช่น: openssl rand -hex 32
JWT_SECRET=เปลี่ยนเป็นค่าสุ่มยาวๆ
